import trpc from "../api/trpc/[trpc]";

const test = () => {
  const test = trpc
  return (
    <div className="m-auto flex h-[65vh] flex-col items-center justify-center text-center text-white">
      <h1 className="gradient-text size-full -mr-2 bg-gradient-to-b from-white to-[#ffffff20] font-display text-9xl font-medium"></h1>
    </div>
  );
};

export default test;
